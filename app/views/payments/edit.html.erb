<% content_for :title, "Editar Pagamento" %>

<div class="max-w-xl space-y-5">
  <div><%= link_to "← Pagamentos", payments_path, class: "text-sm text-gray-500 hover:text-gray-700" %></div>
  <div class="bg-white rounded-xl border border-gray-200 shadow-sm p-6">
    <h2 class="text-xl font-bold text-gray-900 mb-1">Editar Pagamento</h2>
    <p class="text-sm text-gray-500 mb-5"><%= @payment.student.name %></p>
    <%= render "form", payment: @payment %>
  </div>
</div>
