# Copy this file to .env and fill in the values before running docker compose.

# Required — PostgreSQL password for the bjj_admin user
POSTGRES_PASSWORD=change_me

# Required — content of config/master.key
RAILS_MASTER_KEY=

# Optional
MAILER_FROM=no-reply@bjjadmin.com.br
PORT=80
